- name: Fetch Airtable Data
  run: |
    mkdir -p data  # Esto crea la carpeta si no existe
    curl -s -H "Authorization: Bearer ${{ secrets.AIRTABLE_TOKEN }}" \
         "https://api.airtable.com/v0/appo1MtSst3hKF0ky/Vistas" \
         > data/airtable.json
